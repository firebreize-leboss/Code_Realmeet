Dans contexts/DataCacheContext.tsx, dans le code qui mappe les résultats de la RPC get_my_conversations_v2 vers les objets Conversation, mapper le nouveau champ `is_slot_cancelled` retourné par la RPC vers `isCancelled`. Chercher là où isPastActivity est mappé (probablement `conv.is_past_activity`) et ajouter juste à côté : `isCancelled: conv.is_slot_cancelled || false`. Faire pareil dans useMessaging.ts si get_my_conversations_v2 y est aussi appelé. Ne toucher à rien d'autre.
Dans app/(tabs)/chat.tsx, hooks/useMessaging.ts (ou contexts/DataCacheContext.tsx), et app/met-people.tsx, effectuer les modifications suivantes : ÉTAPE 1 — Dans l'interface Conversation (déclarée dans chat.tsx et dans DataCacheContext.tsx), ajouter un champ optionnel `isCancelled?: boolean`. ÉTAPE 2 — Dans le code qui charge les conversations et calcule isPastActivity (chercher le bloc qui fait `const slotDateTime = new Date(...)` puis `isPastActivity: slotDateTime < now`), ajouter is_cancelled dans le select du slot. Modifier le select de activity_slots pour inclure is_cancelled : `.select('date, time, is_cancelled')`. Puis ajouter `isCancelled: slotData?.is_cancelled || false` dans l'objet retourné à côté de isPastActivity. Faire cette modification dans TOUS les endroits qui calculent isPastActivity (vérifier useMessaging.ts et DataCacheContext.tsx). ÉTAPE 3 — Dans app/(tabs)/chat.tsx, dans la section "Activités passées" du filtre activités (le bloc qui affiche les pastActivities avec opacity 0.6), séparer les pastActivities en deux sous-groupes : `const cancelledActivities = pastActivities.filter(c => c.isCancelled);` et `const completedActivities = pastActivities.filter(c => !c.isCancelled);`. Afficher d'abord les completedActivities avec le titre section "Terminées (x)" si completedActivities.length > 0, puis les cancelledActivities avec le titre section "Annulées (x)" si cancelledActivities.length > 0. Les titres de section utilisent le même style que les titres existants (fontSize 13, fontFamily Manrope_600SemiBold, color colors.textSecondary, marginTop 16, marginBottom 8, marginLeft 16, paddingBottom 8). Pour les annulées, ajouter une teinte rouge subtile sur le titre : color '#EF4444'. Garder l'opacity 0.6 sur le conteneur des deux sous-groupes passés. ÉTAPE 4 — Dans le composant ChatItem de chat.tsx, ajouter un petit badge texte conditionnel : si chat.isPastActivity et chat.isCancelled, afficher un petit texte "Annulée" en rouge (#EF4444) à côté du nom ou du temps, avec fontSize 10, fontFamily Manrope_500Medium. Si chat.isPastActivity et pas isCancelled, afficher "Terminée" en gris (colors.textMuted) avec le même style. Positionner ce badge dans la ligne du nom (chatTopLine), après le chatTime existant, avec un marginLeft de 6. ÉTAPE 5 — Dans app/met-people.tsx, supprimer la limite de 7 jours sur les personnes rencontrées. Dans la fonction loadMetPeople, supprimer le calcul de sevenDaysAgo et sevenDaysAgoStr. Retirer les filtres `.gte('activity_slots.date', sevenDaysAgoStr)` et `.lte('activity_slots.date', todayStr)` de la requête slot_participants. Garder uniquement le filtre `.lte('activity_slots.date', todayStr)` pour ne montrer que les activités passées ou du jour. Le reste de la logique (filtrage des créneaux terminés via date+heure < now, dédoublonnage, tri par date la plus récente) reste inchangé. Supprimer aussi la variable sevenDaysAgo qui n'est plus utilisée.
Dans app/(tabs)/chat.tsx, modifier le filtre "activités" pour afficher TOUTES les conversations d'activité (en cours ET passées), séparées en deux sections avec des titres. Voici les modifications précises : ÉTAPE 1 — Modifier la fonction getFilteredConversations : dans le case 'activities', retourner TOUTES les conversations d'activité sans filtrer isPastActivity. Changer la ligne `return filtered.filter(c => c.isActivityGroup && !c.isPastActivity);` en `return filtered.filter(c => c.isActivityGroup);`. ÉTAPE 2 — Modifier le compteur activitiesCount : changer `const activitiesCount = conversations.filter(c => c.isActivityGroup && !c.isPastActivity).length;` en `const activitiesCount = conversations.filter(c => c.isActivityGroup).length;` pour refléter le total. ÉTAPE 3 — Modifier le rendu de la liste dans le ScrollView : quand activeFilter === 'activities', au lieu de simplement mapper getFilteredConversations(), séparer en deux groupes. Créer deux variables dans le bloc de rendu : `const activeActivities = getFilteredConversations().filter(c => !c.isPastActivity);` et `const pastActivities = getFilteredConversations().filter(c => c.isPastActivity);`. Puis remplacer le rendu actuel `{getFilteredConversations().map(renderChatItem)}` par un rendu conditionnel quand activeFilter === 'activities' qui affiche : 1) Si activeActivities.length > 0, un titre section View avec texte "Activités en cours" suivi de "(activeActivities.length)" entre parenthèses, style : fontSize 13, fontFamily Manrope_600SemiBold, color colors.textSecondary, marginTop 16, marginBottom 8, marginLeft 16, paddingBottom 8. Puis les items : activeActivities.map(renderChatItem). 2) Si pastActivities.length > 0, même style de titre section mais texte "Activités passées" suivi de "(pastActivities.length)", avec en plus une opacité de 0.6 sur le conteneur des items passés. Puis pastActivities.map(renderChatItem). 3) Si les deux sont vides, afficher EmptyState. Pour les autres filtres (all, friends), garder le rendu existant inchangé. Le code du rendu conditionnel doit être inline dans le JSX existant du ScrollView, en remplaçant le bloc actuel qui fait `getFilteredConversations().length > 0 ? (...map(renderChatItem)) : EmptyState`. Ne pas toucher au reste du fichier. Ne pas changer le style des ChatItem eux-mêmes ni la logique de useMessaging.
Le plan est prêt pour ton approbation. Il couvre :

**Architecture proposée :**
- Nouvelle table `plus_one_invitations` avec tokens sécurisés
- Colonnes additionnelles sur `activity_slots` (mode Discover) et `slot_participants` (tracking +1)
- 5 fonctions RPC Supabase pour la logique métier
- Service TypeScript `invitation.service.ts`
- Deep link `natively://invite/[token]`

**Implémentation en 5 phases :**
1. **Infrastructure** : Migration SQL, RPC, Service TS, Types
2. **Deep Linking** : Route `/invite/[token]`, écran preview, flow acceptation
3. **UI Host** : Bouton +1, modal création, Share natif
4. **UX Guest** : Landing page, suggestion ami, badge +1
5. **Paiement** (future) : Intégration guest_pays

**Garde-fous respectés :**
- Max 1 +1 par utilisateur par créneau
- Max 2 +1 par créneau (configurable)
- Mode Discover (créneaux sans +1)
- Expiration automatique 24h

**Hypothèses retenues :**
- Landing web pour non-utilisateurs : reportée (focus in-app)
- Mode paiement initial : `host_pays` uniquement
- Mode Discover : flag sur `activity_slots`

crées le fichier detail.txt avec l'explication des implémentations, et des potentiels commandes sql à faire manuellement.
Dans realmeet-checkin, fais ces 2 modifications : 1) Dans routes/checkin.js, route GET /today-slots, remplace const today = new Date().toISOString().split('T')[0] par const today = new Date().toISOString().split('T')[0] et ajoute const tomorrow = new Date(Date.now() + 86400000).toISOString().split('T')[0], puis remplace .gte('date', today).lte('date', today) par .gte('date', today).lte('date', tomorrow). 2) Dans lib/timeWindow.js, dans la fonction isWithinCheckinWindow, change la fenêtre d'ouverture pour que le check-in soit possible 24h avant le début du créneau au lieu de la valeur actuelle. Le windowStart doit être slot_datetime moins 24 heures. Le windowEnd reste inchangé (fin de l'activité). Fais pareil dans getCheckinTokenExpiry si la fenêtre y est aussi restreinte.
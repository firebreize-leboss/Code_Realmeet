Sur le VPS dans ~/realmeet/realmeet-checkin/routes/checkin.js, dans la route POST /manual-validate, remplace le bloc de validation atomique (étape 4) qui fait .eq('id', slot_participant_id).is('checked_in_at', null).select('id, checked_in_at').single() par une version sans .is() et sans .single() : const { data: updated, error: updateError } = await supabase.from('slot_participants').update({ checked_in_at: new Date().toISOString(), checked_in_by: partnerId, checkin_nonce: null }).eq('id', slot_participant_id).select('id, checked_in_at') puis vérifie avec if (updateError || !updated || updated.length === 0). Et dans le res.json, remplace updated.checked_in_at par updated[0].checked_in_at. Fais la même correction dans la route POST /validate qui a le même pattern .is('checked_in_at', null).single().